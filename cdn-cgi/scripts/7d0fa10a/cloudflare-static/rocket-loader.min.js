;(function () {
    function d(n) {
        var t = n.createElement("script");
        return (
            t.async = !0,
            t.setAttribute("data-cfasync", "false"),
            n.body.appendChild(t),
            t
        );
    }

    function p(n) {
        var t = n.getAttribute("data-cfasync");
        return (
            !(t === "" || t === "true") &&
            n.getAttribute("data-cf-modified-") === n.innerHTML
        );
    }

    function b(n) {
        for (var t, e = n.getElementsByTagName("script"); e.length;)
            (t = e[0]), t.hasAttribute("data-cfasync") && e.remove(0);
        return t;
    }

    function g(n) {
        n.src = n.src;
    }

    function S() {
        for (var n, r = document.getElementsByTagName("script"); r.length;)
            (n = r[0]), n.hasAttribute("data-cfasync") && r.remove(0);
    }

    function x(n) {
        for (var t, e = document.getElementsByTagName("script"); e.length;)
            (t = e[0]),
            t.hasAttribute("data-cfasync") &&
                (t.async = !0, t.setAttribute("data-cfasync", "false"));
    }

    function e(n, t) {
        console.warn("Rocket Loader - " + n, t);
    }

    function o(n) {
        var t = n.parentNode;
        t && t.removeChild(n);
    }

    var t = document.currentScript,
        n = t.getAttribute(R);
    if (n) {
        var r = JSON.parse(n.replace(new RegExp("'", "g"), '"'));
        r.script = t,
        r.settings.callback = function () {
            M.inlineScriptsAllowed || S();
        };
        var i = new M(r);
        i.load();
    } else e("Activator script doesn't have settings. No scripts will be executed.");
})();
